// src/app/admin/layout.js
// ============================================================
// Layout antigo do painel /admin.
// Mantido apenas para não quebrar rotas antigas.
// O painel oficial agora é /dashboard.
// ============================================================

export default function AdminLayout({ children }) {
  return children
}